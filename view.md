# 画像ビューモード 仕様書

## 概要

「画像ビューモード」は、バウンディングボックスや機械学習関連の UI を一切表示せず、  
純粋に画像を閲覧することに特化したモードである。

既存の「アノテーションモード」と独立したビューとして実装し、  
ツールバー上のトグルボタンで随時切り替えられる。

---

## 共有データ

以下の既存ストア状態をそのまま参照する（新たなデータ取得は不要）。

| 状態 | 用途 |
|------|------|
| `files: MediaFile[]` | 表示対象ファイルの一覧 |
| `currentDir` | フォルダが開かれているかの判定 |

ファイルリストはディレクトリ区切りなしの**フラット配列**として扱う。  
表示対象は `media_type === 'image'` のもののみ（動画・その他は非表示）。

---

## モード切替

### 切替ボタン

- ツールバー左端のフォルダボタン群の右隣に「ビューモード」トグルボタンを配置
- アイコン: グリッド状の正方形（例: `⊞`）
- `currentDir === null` のときはボタンを無効化（グレーアウト）

### ルーティング / 状態

- ストアに `viewMode: 'annotate' | 'view'` 状態を追加
- デフォルト値: `'annotate'`
- トグルボタン押下で `'annotate'` ↔ `'view'` を切り替える
- モード変更時に `viewImageIndex` は保持（最後に選択していた画像を維持）

---

## 画面構成

```
┌──────────────────────────────────────────────────────────┐
│ Toolbar（既存。推論ボタン・保存ボタン等は非表示）         │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  グリッド選択画面 または 大表示画面                       │
│  （viewSubMode に応じて切り替わる）                       │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

ビューモード中はファイルリストパネル・MetadataPanel は**非表示**とする。  
ツールバーは共有するが、以下のボタンをビューモード中は隠す（またはレンダリングしない）:

- 物体検出 / 顔検出 / NudeNet 部位検出 / NSFW 判定 ボタン（各 ⚙ 設定パネルを含む）
- 一括 NudeNet ボタン
- 学習データとして保存 ボタン
- アノテーションモード ON/OFF トグル

---

## グリッド選択画面

### レイアウト

- 画面いっぱいにサムネイルグリッドを表示
- 各サムネイルは**正方形**（`aspect-ratio: 1 / 1`）、`object-fit: cover`
- カラム数は画面幅に応じて可変（例: 幅 1200px → 6列, 800px → 4列）
  - `minmax(160px, 1fr)` の CSS Grid auto-fill が基本方針
- 選択中アイテムにはフォーカスリングを表示

### 表示内容（各セル）

```
┌──────────────┐
│              │
│   thumbnail  │
│              │
│──────────────│
│ ファイル名.jpg│  ← 1行、overflow: ellipsis
└──────────────┘
```

### マウス操作

| 操作 | 動作 |
|------|------|
| サムネイルをクリック | そのアイテムを選択（フォーカス移動） |
| サムネイルをダブルクリック | 大表示画面に遷移 |

### キーボード操作

| キー | 動作 |
|------|------|
| `←` / `→` | 前 / 次のアイテムを選択 |
| `↑` / `↓` | 1行上 / 下のアイテムを選択 |
| `Enter` | 選択中アイテムを大表示画面で開く |
| `Home` | 先頭アイテムへ移動 |
| `End` | 末尾アイテムへ移動 |

---

## 大表示画面

### レイアウト

- 画像を中央に表示し、利用可能領域いっぱいに拡大（`object-fit: contain`）
- 背景は黒または暗いグレー
- 左右の矢印ボタン（`<` / `>`）を画面両端に常時表示
- 左上に「← グリッドに戻る」ボタン

### マウス操作

| 操作 | 動作 |
|------|------|
| 左矢印ボタンをクリック | 前の画像 |
| 右矢印ボタンをクリック | 次の画像 |
| 「← 戻る」ボタンをクリック | グリッド選択画面に戻る |

### キーボード操作

| キー | 動作 |
|------|------|
| `←` / `↑` | 前の画像 |
| `→` / `↓` | 次の画像 |
| `Escape` | グリッド選択画面に戻る |

### 補助情報

- 画像下部（またはオーバーレイ）に `{現在インデックス + 1} / {総枚数}` を表示
- ファイル名を同行に表示

---

## ストア変更

```typescript
// 追加する状態
viewMode: 'annotate' | 'view'      // デフォルト: 'annotate'
viewSubMode: 'grid' | 'large'      // デフォルト: 'grid'
viewImageIndex: number             // 大表示中の画像インデックス。デフォルト: 0

// 追加するアクション
setViewMode: (mode: 'annotate' | 'view') => void
setViewSubMode: (sub: 'grid' | 'large') => void
setViewImageIndex: (index: number) => void
openInLargeView: (index: number) => void  // setViewSubMode('large') + setViewImageIndex(index)
```

---

## コンポーネント構成

```
src/components/
├── ViewModeGrid.tsx      グリッド選択画面
├── ViewModeLarge.tsx     大表示画面
└── ViewModeToggle.tsx    ツールバー内トグルボタン（既存 Toolbar.tsx に組み込み）
```

`App.tsx` または既存のメインレイアウトにて:

```tsx
{viewMode === 'view' ? (
  viewSubMode === 'grid' ? <ViewModeGrid /> : <ViewModeLarge />
) : (
  /* 既存のアノテーションレイアウト */
)}
```

---

## 意図的に含まないもの

- バウンディングボックス表示・編集
- 推論ボタン（顔検出 / 物体検出 / NudeNet / NSFW）
- MetadataPanel（評価・タグ・BB リスト・学習パネル）
- ファイルリストサイドパネル
- ズーム・パン操作（大表示は `object-fit: contain` のみ）
- ファイル削除・移動などの破壊的操作

---

## 実装優先度

| フェーズ | 内容 |
|---------|------|
| Phase 1 | ストア状態追加 + トグルボタン + グリッド選択画面（クリック選択 + 矢印キー） |
| Phase 2 | 大表示画面（Enter/ダブルクリックで遷移、ESC で戻る） |
| Phase 3 | スタイル調整（レスポンシブカラム数、フォーカスリング、遷移アニメーション） |
